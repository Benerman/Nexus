{
  "name": "nexus-smoke-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Production smoke tests for Nexus",
  "scripts": {
    "test": "jest --forceExit --detectOpenHandles",
    "test:ci": "jest --forceExit --detectOpenHandles --ci --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "socket.io-client": "^4.6.1"
  }
}

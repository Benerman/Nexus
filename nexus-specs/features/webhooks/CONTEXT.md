# Webhooks Feature Context

Webhooks are external trust boundaries — they accept unauthenticated HTTP POST requests from the internet. The token in the URL is the only authentication. Any change to webhook routing, token validation, or rate limiting carries high security risk.

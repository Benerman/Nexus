# Social Edge Cases

- Friend request to blocked user — rejected
- Self-friend request — rejected
- Block removes existing friendship
- Block prevents future DMs (server enforces)
- Double-accept of friend request — idempotent

# DMs Feature Context

DMs are user-to-user and are scoped outside of any server. The message request system prevents spam from unknown users.

Blocked users must never be able to message each other. This check must be server-side.

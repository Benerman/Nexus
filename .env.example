# Giphy API Key (get one at https://developers.giphy.com/)
GIPHY_API_KEY=your-giphy-api-key-here

# Database credentials (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-to-a-strong-password

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=change-this-to-a-random-secret

# Client URL (your deployed frontend URL)
CLIENT_URL=http://localhost:3000

# Additional allowed CORS origins (comma-separated, optional)
# Use this if your frontend is accessible from multiple URLs
# ALLOWED_ORIGINS=https://example.com,https://www.example.com
